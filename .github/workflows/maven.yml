name: CI/CD Spring Boot avec Render

on:
  push:
    branches: [ "main" ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      # 1️⃣ Checkout
      - name: 📥 Checkout du code
        uses: actions/checkout@v3

      # 2️⃣ Setup Java 21 + Maven
      - name: Setup Java 21
        uses: actions/setup-java@v3
        with:
          distribution: temurin
          java-version: 21

      # 3️⃣ Vérification des secrets
      - name: 🔒 Vérification des secrets
        run: |
          if [ -z "${{ secrets.RENDER_SERVICE_ID }}" ]; then
            echo "ERROR: RENDER_SERVICE_ID n'est pas défini !"
            exit 1
          fi
          if [ -z "${{ secrets.RENDER_API_KEY }}" ]; then
            echo "ERROR: RENDER_API_KEY n'est pas défini !"
            exit 1
          fi

 - name: Cache Maven Dependencies
        uses: actions/cache@v3
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-maven-

      - name: Run Maven Integration Tests
        run: mvn clean verify site

      - name: Upload JUnit Test Results
        if: always()
        uses: actions/upload-artifact@v4 # Updated to v4 (or a newer version)
        with:
          name: junit-xml-reports
          path: |
            **/target/surefire-reports/*.xml
            **/target/failsafe-reports/TEST-*.xml

      - name: Upload HTML Test Reports
        if: always()
        uses: actions/upload-artifact@v4 # Updated to v4 (or a newer version)
        with:
          name: html-test-reports
          path: |
            **/target/site

      # 5️⃣ Build Dockerfile pour Render (skip tests)
      - name: Build Docker image
        run: docker build -t myapp --target runtime .

      # 6️⃣ Installer CA certificates pour curl HTTPS
      - name: Installer CA certificates
        run: sudo apt-get update && sudo apt-get install -y ca-certificates

      # 7️⃣ Déploiement Render
      - name: 🚀 Déclencher le déploiement Render
        run: |
          curl -v -X POST "https://api.render.com/v1/services/${{ secrets.RENDER_SERVICE_ID }}/deploys" \
            -H "Accept: application/json" \
            -H "Authorization: Bearer ${{ secrets.RENDER_API_KEY }}"
