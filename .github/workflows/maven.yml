name: CI/CD Spring Boot avec Render

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    env:  # 
      SPRING_PROFILES_ACTIVE: prod

      DB_URL: ${{ secrets.DB_URL }}
      DB_USERNAME: elec_business_user
      DB_PASSWORD: ${{ secrets.DB_PASSWORD }}

      JWT_SECRET: ${{ secrets.JWT_SECRET}}
      JWT_EXPIRATION: 3600000

      MAIL_HOST: smtp.gmail.com
      MAIL_PORT: 587
      MAIL_USERNAME: ${{ secrets.MAIL_USERNAME}}
      MAIL_PASSWORD: ${{ secrets.SMTP_PASSWORD }}

      REDIS_HOST: ${{ secrets.REDIS_HOST}}
      REDIS_PORT: ${{ secrets.REDIS_PORT}}

      EMAIL_VERIFICATION_REQUIRED: true

      URL_FRONT: ${{ secrets.URL_FRONT}}

    steps:
      # 1️⃣ Checkout
      - name: 📥 Checkout du code
        uses: actions/checkout@v3

      # 2️⃣ Setup Java 21 (pour build + tests)
      - name: Setup Java 21
        uses: actions/setup-java@v3
        with:
          distribution: temurin
          java-version: 21

      # 3️⃣ Vérification des secrets Render
      - name: 🔒 Vérification des secrets
        run: |
          if [ -z "${{ secrets.RENDER_SERVICE_ID }}" ]; then
            echo "ERROR: RENDER_SERVICE_ID n'est pas défini !"
            exit 1
          fi
          if [ -z "${{ secrets.RENDER_API_KEY }}" ]; then
            echo "ERROR: RENDER_API_KEY n'est pas défini !"
            exit 1
          fi

      # 4️⃣ Cache Maven
      - name: Cache Maven Dependencies
        uses: actions/cache@v3
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-maven-

      # 5️⃣ Run Maven Tests (unit + integration)
      - name: Run Maven Integration Tests
        run: mvn clean verify site

      # 6️⃣ Upload JUnit Test Results
      - name: Upload JUnit Test Results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: junit-xml-reports
          path: |
            **/target/surefire-reports/*.xml
            **/target/failsafe-reports/TEST-*.xml

      # 7️⃣ Upload HTML Test Reports
      - name: Upload HTML Test Reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: html-test-reports
          path: |
            **/target/site

      # 8️⃣ Build Docker image pour Render (skip tests)
      - name: Build Docker image
        run: docker build -t myapp --target runtime .

      # 9️⃣ Installer CA certificates pour curl HTTPS
      - name: Installer CA certificates
        run: sudo apt-get update && sudo apt-get install -y ca-certificates

      # 🔟 Déploiement Render via API
      - name: Trigger deploy on Render.com
        run: |
          curl --request POST \
            --url https://api.render.com/v1/services/${{ secrets.RENDER_SERVICE_ID }}/deploys \
            --header 'accept: application/json' \
            --header 'content-type: application/json' \
            --header "Authorization: Bearer ${{ secrets.RENDER_API_KEY }}" \
            --data '{
              "clearCache": "do_not_clear",
              "commitId": "string",
              "imageUrl": "string"
            }'
