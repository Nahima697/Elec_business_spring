spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

email-verification.required=false

# === Logs SQL ===
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# === ENV ===
app.auth.fronttend.url=http://localhost:4200
application.api.url=http://localhost:8080


spring.sendgrid.api-key=${SENDGRID_API_KEY}

# Garde le log pour voir si ça part
logging.level.org.springframework.mail=DEBUG

# Cloudinary & autres
cloudinary.cloud-name=test-cloud
cloudinary.api-key=123456789
cloudinary.api-secret=test-secret-key
FROM_EMAIL=nahima.toumi697@gmail.com